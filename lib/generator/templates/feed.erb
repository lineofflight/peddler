# frozen_string_literal: true

# <%= Generator::Config::GENERATED_FILE_NOTICE %>

module Peddler
  module Feeds
<% if schema["title"] || schema["description"] -%>
    # <%= schema["title"] %>
    #
<%= split_long_comment_line(schema["description"], base_indent: 4) if schema["description"] %>
<% end -%>
    module <%= class_name %>
      class << self
        def parse(hash)
          Feed.parse(hash)
        end
      end
    end
  end
end
